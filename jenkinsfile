pipeline {
    agent any
        stage("Build Docker Image") {
            steps {
                sh 'docker build -t app_image_2 .'
            }
        }
        stage("Run Container") {
            steps {
                sh 'docker run -p 5000:5000 image_app_2'
            }
        }
    }

